services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: strech
      POSTGRES_PASSWORD: strech
      POSTGRES_DB: strech_dispatch
    ports:
      - '5432:5432'
    volumes:
      - strech_pg:/var/lib/postgresql/data

volumes:
  strech_pg:
