# Postgres (Neon or local)
DATABASE_URL=postgresql://strech:strech@127.0.0.1:5432/strech_dispatch

# Edge + actor auth (must match ops console)
EDGE_BEARER_TOKEN=replace-me-edge-secret
ACTOR_JWT_SECRET=replace-me-hs256-secret

PORT=3001
HOST=0.0.0.0
